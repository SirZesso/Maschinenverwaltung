CREATE TABLE AREA (
  ID INT NOT NULL,SITE INT NOT NULL,DESCRIPTION LINESTRING NULL,FLOOR LINESTRING NULL,SURFACE DOUBLE NULL,LAYOUT LINESTRING NULL,MANAGER LINESTRING NULL,PRIMARY KEY (ID)
);

CREATE TABLE ENTERPRISE (
  ID INT NOT NULL,NAME LINESTRING NULL,PRIMARY KEY (ID)
);

CREATE TABLE LASER_MACHINE (
  ID INT NOT NULL,MACHINE INT NOT NULL,PRIMARY KEY (ID)
);

CREATE TABLE MACHINE (
  ID INT NOT NULL,CUSTOMER_ID INT NOT NULL,MANUFACTURER_ID INT NOT NULL,TYPE LINESTRING NOT NULL,AREA INT NULL,PRIMARY KEY (ID)
);

CREATE TABLE PRESS_MACHINE (
  ID INT NOT NULL,MACHINE INT NOT NULL,PRIMARY KEY (ID)
);

CREATE TABLE SITE (
  ID INT NOT NULL,ENTERPRISE INT NOT NULL,STREET LINESTRING NULL,STREET_NR INT NULL,CITY LINESTRING NULL,POSTALCODE INT NULL,PRIMARY KEY (ID)
);

ALTER TABLE MACHINE ADD CONSTRAINT FK_ENTERPRISE_TO_MACHINE FOREIGN KEY (CUSTOMER_ID) REFERENCES ENTERPRISE (ID);

ALTER TABLE MACHINE ADD CONSTRAINT FK_ENTERPRISE_TO_MACHINE1 FOREIGN KEY (MANUFACTURER_ID) REFERENCES ENTERPRISE (ID);

ALTER TABLE AREA ADD CONSTRAINT FK_SITE_TO_AREA FOREIGN KEY (SITE) REFERENCES SITE (ID);

ALTER TABLE LASER_MACHINE ADD CONSTRAINT FK_MACHINE_TO_LASER_MACHINE FOREIGN KEY (MACHINE) REFERENCES MACHINE (ID);

ALTER TABLE PRESS_MACHINE ADD CONSTRAINT FK_MACHINE_TO_PRESS_MACHINE FOREIGN KEY (MACHINE) REFERENCES MACHINE (ID);

ALTER TABLE SITE ADD CONSTRAINT FK_ENTERPRISE_TO_SITE FOREIGN KEY (ENTERPRISE) REFERENCES ENTERPRISE (ID);

ALTER TABLE MACHINE ADD CONSTRAINT FK_AREA_TO_MACHINE FOREIGN KEY (AREA) REFERENCES AREA (ID);